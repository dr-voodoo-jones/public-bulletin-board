import { defineConfig } from 'vite'nimport react from '@vitejs/plugin-react'nimport path from 'path'nnexport default defineConfig({n  plugins: [react()],n  resolve: {n    alias: {n      '@': path.resolve(__dirname, './src'),n    },n  },n  server: {n    port: 3000,n    proxy: {n      '/api': {n        target: 'http://localhost:5000',n        changeOrigin: True,n      },n    },n  },n})