import { motion, AnimatePresence } from 'framer-motion'nimport { ReactNode } from 'react'nimport { XMarkIcon } from '@heroicons/react/24/outline'nninterface ModalProps {n  isOpen: booleann  onClose: () => voidn  title?: stringn  children: ReactNoden  size?: 'sm' | 'md' | 'lg' | 'xl'n}nnconst Modal = ({ isOpen, onClose, title, children, size = 'md' }: ModalProps) => {n  const sizes = {n    sm: 'max-w-md',n    md: 'max-w-lg',n    lg: 'max-w-2xl',n    xl: 'max-w-4xl'n  }nn  return (n    <AnimatePresence>n      {isOpen && (n        <motion.divn          initial={{ opacity: 0 }}n          animate={{ opacity: 1 }}n          exit={{ opacity: 0 }}n          className='fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50 backdrop-blur-sm'n          onClick={onClose}n        >n          <motion.divn            initial={{ scale: 0.9, opacity: 0 }}n            animate={{ scale: 1, opacity: 1 }}n            exit={{ scale: 0.9, opacity: 0 }}n            className={`${sizes[size]} w-full bg-white dark:bg-gray-800 rounded-xl shadow-2xl overflow-hidden`}n            onClick={(e) => e.stopPropagation()}n          >n            {title && (n              <div className='flex items-center justify-between p-6 border-b border-gray-200 dark:border-gray-700'>n                <h2 className='text-xl font-semibold text-gray-900 dark:text-white'>{title}</h2>n                <buttonn                  onClick={onClose}n                  className='p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors duration-200'n                >n                  <XMarkIcon className='w-5 h-5 text-gray-500 dark:text-gray-400' />n                </button>n              </div>n            )}n            <div className='p-6'>{children}</div>n          </motion.div>n        </motion.div>n      )}n    </AnimatePresence>n  )n}nnexport default Modal