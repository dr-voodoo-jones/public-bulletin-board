import { motion } from 'framer-motion'nimport { ReactNode } from 'react'nninterface ButtonProps {n  children: ReactNoden  variant?: 'primary' | 'secondary' | 'outline' | 'ghost'n  size?: 'sm' | 'md' | 'lg'n  onClick?: () => voidn  disabled?: booleann  type?: 'button' | 'submit' | 'reset'n  className?: stringn  icon?: ReactNoden}nnconst Button = ({n  children,n  variant = 'primary',n  size = 'md',n  onClick,n  disabled = false,n  type = 'button',n  className = '',n  iconn}: ButtonProps) => {n  const baseClasses = 'inline-flex items-center justify-center font-semibold rounded-lg transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed'nn  const variants = {n    primary: 'bg-primary-500 hover:bg-primary-600 text-white focus:ring-primary-500 shadow-lg hover:shadow-xl',n    secondary: 'bg-secondary-500 hover:bg-secondary-600 text-white focus:ring-secondary-500 shadow-lg hover:shadow-xl',n    outline: 'border-2 border-primary-500 text-primary-500 hover:bg-primary-500 hover:text-white focus:ring-primary-500',n    ghost: 'text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 focus:ring-gray-500'n  }nn  const sizes = {n    sm: 'px-3 py-1.5 text-sm',n    md: 'px-4 py-2 text-base',n    lg: 'px-6 py-3 text-lg'n  }nn  const classes = `${baseClasses} ${variants[variant]} ${sizes[size]} ${className}`nn  return (n    <motion.buttonn      type={type}n      onClick={onClick}n      disabled={disabled}n      className={classes}n      whileHover={{ scale: disabled ? 1 : 1.05 }}n      whileTap={{ scale: disabled ? 1 : 0.95 }}n    >n      {icon && <span className='mr-2'>{icon}</span>}n      {children}n    </motion.button>n  )n}nnexport default Button