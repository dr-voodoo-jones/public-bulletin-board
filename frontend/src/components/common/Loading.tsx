import { motion } from 'framer-motion'nninterface LoadingProps {n  size?: 'sm' | 'md' | 'lg'n  text?: stringn}nnconst Loading = ({ size = 'md', text = 'Loading...' }: LoadingProps) => {n  const sizes = {n    sm: 'w-4 h-4',n    md: 'w-8 h-8',n    lg: 'w-12 h-12'n  }nn  return (n    <div className='flex flex-col items-center justify-center space-y-4 py-8'>n      <motion.divn        className={`${sizes[size]} border-4 border-primary-200 border-t-primary-500 rounded-full`}n        animate={{ rotate: 360 }}n        transition={{ duration: 1, repeat: Infinity, ease: 'linear' }}n      />n      {text && (n        <motion.pn          initial={{ opacity: 0 }}n          animate={{ opacity: 1 }}n          className='text-gray-600 dark:text-gray-400 text-sm'n        >n          {text}n        </motion.p>n      )}n    </div>n  )n}nnexport default Loading