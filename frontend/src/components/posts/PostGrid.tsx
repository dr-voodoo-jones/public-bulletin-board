import { motion } from 'framer-motion'nimport PostCard from './PostCard.tsx'nimport { Post } from '../../types/post.ts'nninterface PostGridProps {n  posts: Post[]n}nnconst PostGrid = ({ posts }: PostGridProps) => {n  const containerVariants = {n    hidden: { opacity: 0 },n    visible: {n      opacity: 1,n      transition: {n        staggerChildren: 0.1n      }n    }n  }nn  return (n    <motion.divn      variants={containerVariants}n      initial='hidden'n      animate='visible'n      className='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6'n    >n      {posts.map((post, index) => (n        <motion.divn          key={post.id}n          variants={{n            hidden: { opacity: 0, y: 20 },n            visible: { opacity: 1, y: 0 }n          }}n          transition={{ delay: index * 0.1 }}n        >n          <PostCard post={post} />n        </motion.div>n      ))}n    </motion.div>n  )n}nnexport default PostGrid