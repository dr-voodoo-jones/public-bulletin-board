import { motion } from 'framer-motion'nimport { Link } from 'react-router-dom'nimport { HeartIcon, EyeIcon, ChatBubbleLeftIcon } from '@heroicons/react/24/outline'nimport { HeartIcon as HeartSolidIcon } from '@heroicons/react/24/solid'nimport { useState } from 'react'nimport { Post } from '../../types/post.ts'nninterface PostCardProps {n  post: Postn}nnconst PostCard = ({ post }: PostCardProps) => {n  const [isLiked, setIsLiked] = useState(false)n  const [likes, setLikes] = useState(post.likes)nn  const handleLike = () => {n    setIsLiked(!isLiked)n    setLikes(isLiked ? likes - 1 : likes + 1)n  }nn  const formatDate = (dateString: string) => {n    const date = new Date(dateString)n    const now = new Date()n    const diffInHours = Math.floor((now.getTime() - date.getTime()) / (1000 * 60 * 60))nn    if (diffInHours < 1) return 'Just now'n    if (diffInHours < 24) return `${diffInHours}h ago'n    if (diffInHours < 48) return 'Yesterday'n    return date.toLocaleDateString()n  }nn  return (n    <motion.divn      whileHover={{ y: -5 }}n      className='card overflow-hidden group cursor-pointer'n    >n      <Link to={`/post/${post.id}`}>n        {/* Image Section */}n        {post.images.length > 0 && (n          <div className='relative h-48 overflow-hidden'>n            <motion.imgn              src={post.images[0]}n              alt={post.title}n              className='w-full h-full object-cover group-hover:scale-105 transition-transform duration-300'n              loading='lazy'n            />n            <div className='absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300' />n          </div>n        )}nn        {/* Content Section */}n        <div className='p-6'>n          <div className='flex items-start justify-between mb-3'>n            <h3 className='text-lg font-semibold text-gray-900 dark:text-white line-clamp-2 group-hover:text-primary-500 transition-colors duration-200'>n              {post.title}n            </h3>n            <span className='text-2xl ml-2'>{post.title.includes('🌟') ? '🌟' : post.title.includes('🍕') ? '🍕' : '📝'}</span>n          </div>nn          <p className='text-gray-600 dark:text-gray-400 text-sm line-clamp-3 mb-4'>n            {post.content}n          </p>nn          {/* Tags */}n          {post.tags.length > 0 && (n            <div className='flex flex-wrap gap-2 mb-4'>n              {post.tags.slice(0, 3).map((tag) => (n                <spann                  key={tag}n                  className='px-2 py-1 bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-400 text-xs rounded-full'n                >n                  #{tag}n                </span>n              ))}n              {post.tags.length > 3 && (n                <span className='px-2 py-1 bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-400 text-xs rounded-full'>n                  +{post.tags.length - 3}n                </span>n              )}n            </div>n          )}nn          {/* Meta Information */}n          <div className='flex items-center justify-between text-sm text-gray-500 dark:text-gray-400'>n            <div className='flex items-center space-x-1'>n              <span className='w-6 h-6 bg-gradient-to-r from-primary-400 to-secondary-400 rounded-full flex items-center justify-center text-white text-xs font-bold'>n                {post.author.charAt(0).toUpperCase()}n              </span>n              <span>{post.author}</span>n            </div>n            <span>{formatDate(post.createdAt)}</span>n          </div>n        </div>n      </Link>nn      {/* Action Bar */}n      <div className='px-6 pb-4 flex items-center justify-between'>n        <div className='flex items-center space-x-4'>n          <motion.buttonn            whileTap={{ scale: 0.9 }}n            onClick={handleLike}n            className='flex items-center space-x-1 text-gray-500 dark:text-gray-400 hover:text-red-500 transition-colors duration-200'n          >n            {isLiked ? (n              <HeartSolidIcon className='w-5 h-5 text-red-500' />n            ) : (n              <HeartIcon className='w-5 h-5' />n            )}n            <span className='text-sm'>{likes}</span>n          </motion.button>nn          <div className='flex items-center space-x-1 text-gray-500 dark:text-gray-400'>n            <EyeIcon className='w-5 h-5' />n            <span className='text-sm'>{post.views}</span>n          </div>nn          <div className='flex items-center space-x-1 text-gray-500 dark:text-gray-400'>n            <ChatBubbleLeftIcon className='w-5 h-5' />n            <span className='text-sm'>0</span>n          </div>n        </div>nn        <motion.divn          whileHover={{ scale: 1.1 }}n          className='text-primary-500 font-medium text-sm'n        >n          Read more →n        </motion.div>n      </div>n    </motion.div>n  )n}nnexport default PostCard