import { motion } from 'framer-motion'nimport { MagnifyingGlassIcon, FunnelIcon } from '@heroicons/react/24/outline'nimport { PostFilters as PostFiltersType } from '../../types/post.ts'nninterface PostFiltersProps {n  filters: PostFiltersTypen  onFiltersChange: (filters: PostFiltersType) => voidn}nnconst PostFilters = ({ filters, onFiltersChange }: PostFiltersProps) => {n  const popularTags = ['adventure', 'food', 'nature', 'art', 'travel', 'cooking', 'books', 'music']nn  const handleSearchChange = (value: string) => {n    onFiltersChange({ ...filters, search: value })n  }nn  const handleTagToggle = (tag: string) => {n    const newTags = filters.tags.includes(tag)n      ? filters.tags.filter(t => t !== tag)n      : [...filters.tags, tag]n    onFiltersChange({ ...filters, tags: newTags })n  }nn  const handleSortChange = (sortBy: 'newest' | 'oldest' | 'popular') => {n    onFiltersChange({ ...filters, sortBy })n  }nn  return (n    <motion.divn      initial={{ opacity: 0, y: 20 }}n      animate={{ opacity: 1, y: 0 }}n      className='bg-white/50 dark:bg-gray-800/50 backdrop-blur-md rounded-xl p-6 border border-white/20 dark:border-gray-700/20'n    >n      <div className='space-y-6'>n        {/* Search Bar */}n        <div className='relative'>n          <MagnifyingGlassIcon className='absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400' />n          <inputn            type='text'n            placeholder='Search posts...'n            value={filters.search}n            onChange={(e) => handleSearchChange(e.target.value)}n            className='w-full pl-10 pr-4 py-3 bg-white dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent dark:text-white transition-all duration-200'n          />n        </div>nn        {/* Tags Filter */}n        <div>n          <div className='flex items-center space-x-2 mb-3'>n            <FunnelIcon className='w-5 h-5 text-gray-600 dark:text-gray-400' />n            <h3 className='font-semibold text-gray-800 dark:text-gray-200'>Filter by Tags</h3>n          </div>n          <div className='flex flex-wrap gap-2'>n            {popularTags.map((tag) => (n              <motion.buttonn                key={tag}n                whileHover={{ scale: 1.05 }}n                whileTap={{ scale: 0.95 }}n                onClick={() => handleTagToggle(tag)}n                className={`px-3 py-1.5 rounded-full text-sm font-medium transition-all duration-200 ${n                  filters.tags.includes(tag)n                    ? 'bg-primary-500 text-white shadow-lg'n                    : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600'n                }`}n              >n                #{tag}n              </motion.button>n            ))}n          </div>n        </div>nn        {/* Sort Options */}n        <div>n          <h3 className='font-semibold text-gray-800 dark:text-gray-200 mb-3'>Sort by</h3>n          <div className='flex space-x-2'>n            {[n              { value: 'newest', label: 'Newest', icon: '🆕' },n              { value: 'oldest', label: 'Oldest', icon: '📅' },n              { value: 'popular', label: 'Popular', icon: '🔥' }n            ].map((option) => (n              <motion.buttonn                key={option.value}n                whileHover={{ scale: 1.05 }}n                whileTap={{ scale: 0.95 }}n                onClick={() => handleSortChange(option.value as 'newest' | 'oldest' | 'popular')}n                className={`flex items-center space-x-1 px-3 py-2 rounded-lg text-sm font-medium transition-all duration-200 ${n                  filters.sortBy === option.valuen                    ? 'bg-secondary-500 text-white shadow-lg'n                    : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600'n                }`}n              >n                <span>{option.icon}</span>n                <span>{option.label}</span>n              </motion.button>n            ))}n          </div>n        </div>n      </div>n    </motion.div>n  )n}nnexport default PostFilters