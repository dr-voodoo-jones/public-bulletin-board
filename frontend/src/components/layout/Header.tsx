import { motion } from 'framer-motion'nimport { Link, useLocation } from 'react-router-dom'nimport { useTheme } from '../../context/ThemeContext.tsx'nimport { SunIcon, MoonIcon, ComputerDesktopIcon } from '@heroicons/react/24/outline'nnconst Header = () => {n  const { theme, toggleTheme } = useTheme()n  const location = useLocation()nn  const navItems = [n    { path: '/', label: 'Home', icon: '🏠' },n    { path: '/create', label: 'Create Post', icon: '✏️' },n    { path: '/about', label: 'About', icon: 'ℹ️' }n  ]nn  const getThemeIcon = () => {n    switch (theme) {n      case 'light':n        return <SunIcon className='w-5 h-5' />n      case 'dark':n        return <MoonIcon className='w-5 h-5' />n      default:n        return <ComputerDesktopIcon className='w-5 h-5' />n    }n  }nn  return (n    <motion.headern      initial={{ y: -100 }}n      animate={{ y: 0 }}n      className='sticky top-0 z-50 glass-effect border-b border-white/20 dark:border-gray-700/20'n    >n      <div className='container mx-auto px-4 py-4'>n        <div className='flex items-center justify-between'>n          <Link to='/' className='flex items-center space-x-2 group'>n            <motion.divn              whileHover={{ rotate: 360 }}n              transition={{ duration: 0.5 }}n              className='text-3xl'n            >n              📌n            </motion.div>n            <div className='hidden sm:block'>n              <h1 className='text-xl font-bold text-gradient'>Public Bulletin Board</h1>n              <p className='text-xs text-gray-600 dark:text-gray-400'>Share your stories with the world</p>n            </div>n          </Link>nn          <nav className='flex items-center space-x-4'>n            {navItems.map((item) => (n              <Linkn                key={item.path}n                to={item.path}n                className={`flex items-center space-x-1 px-3 py-2 rounded-lg transition-all duration-200 ${n                  location.pathname === item.pathn                    ? 'bg-primary-500 text-white shadow-lg'n                    : 'hover:bg-primary-100 dark:hover:bg-primary-900/20 text-gray-700 dark:text-gray-300'n                }`}n              >n                <span className='text-lg'>{item.icon}</span>n                <span className='hidden md:inline font-medium'>{item.label}</span>n              </Link>n            ))}nn            <motion.buttonn              whileHover={{ scale: 1.1 }}n              whileTap={{ scale: 0.9 }}n              onClick={toggleTheme}n              className='p-2 rounded-lg bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors duration-200'n              title={`Switch to ${theme === 'light' ? 'dark' : theme === 'dark' ? 'auto' : 'light'} mode`}n            >n              {getThemeIcon()}n            </motion.button>n          </nav>n        </div>n      </div>n    </motion.header>n  )n}nnexport default Header