import { motion } from 'framer-motion'nimport { Link } from 'react-router-dom'nimport { PlusIcon } from '@heroicons/react/24/outline'nnconst FloatingButton = () => {n  return (n    <motion.divn      className='fixed bottom-6 right-6 z-40'n      initial={{ scale: 0, opacity: 0 }}n      animate={{ scale: 1, opacity: 1 }}n      transition={{ delay: 0.5, type: 'spring', stiffness: 200 }}n    >n      <Link to='/create'>n        <motion.buttonn          whileHover={{ scale: 1.1, rotate: 90 }}n          whileTap={{ scale: 0.9 }}n          className='w-14 h-14 bg-gradient-to-r from-primary-500 to-secondary-500 text-white rounded-full shadow-lg hover:shadow-xl flex items-center justify-center transition-all duration-200'n        >n          <PlusIcon className='w-6 h-6' />n        </motion.button>n      </Link>n    </motion.div>n  )n}nnexport default FloatingButton