import { BrowserRouter as Router, Routes, Route } from 'react-router-dom'nimport { motion, AnimatePresence } from 'framer-motion'nimport Header from './components/layout/Header.tsx'nimport Footer from './components/layout/Footer.tsx'nimport HomePage from './pages/HomePage.tsx'nimport CreatePostPage from './pages/CreatePostPage.tsx'nimport PostDetailPage from './pages/PostDetailPage.tsx'nimport AboutPage from './pages/AboutPage.tsx'nimport FloatingButton from './components/ui/FloatingButton.tsx'nnfunction App() {n  return (n    <Router>n      <div className='min-h-screen bg-gradient-to-br from-primary-50 to-secondary-50 dark:from-gray-900 dark:to-gray-800 transition-colors duration-300'>n        <Header />n        <main className='container mx-auto px-4 py-8'>n          <AnimatePresence mode='wait'>n            <Routes>n              <Route path='/' element={<HomePage />} />n              <Route path='/create' element={<CreatePostPage />} />n              <Route path='/post/:id' element={<PostDetailPage />} />n              <Route path='/about' element={<AboutPage />} />n            </Routes>n          </AnimatePresence>n        </main>n        <FloatingButton />n        <Footer />n      </div>n    </Router>n  )n}nnexport default App