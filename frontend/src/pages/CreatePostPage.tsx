import { motion } from 'framer-motion'nimport { useState } from 'react'nimport { useNavigate } from 'react-router-dom'nimport PostForm from '../components/posts/PostForm.tsx'nimport Button from '../components/common/Button.tsx'nimport { ArrowLeftIcon } from '@heroicons/react/24/outline'nnconst CreatePostPage = () => {n  const navigate = useNavigate()n  const [isSubmitting, setIsSubmitting] = useState(false)nn  const handleSubmit = async (formData: any) => {n    setIsSubmitting(true)n    // Simulate API calln    await new Promise(resolve => setTimeout(resolve, 2000))n    setIsSubmitting(false)n    navigate('/')n  }nn  return (n    <motion.divn      initial={{ opacity: 0, y: 20 }}n      animate={{ opacity: 1, y: 0 }}n      className='max-w-4xl mx-auto space-y-8'n    >n      {/* Header */}n      <div className='flex items-center space-x-4'>n        <Buttonn          variant='ghost'n          onClick={() => navigate('/')}n          icon={<ArrowLeftIcon className='w-5 h-5' />}n        >n          Backn        </Button>n        <div>n          <h1 className='text-3xl font-bold text-gradient'>Create a New Post</h1>n          <p className='text-gray-600 dark:text-gray-400 mt-2'>n            Share your story, pictures, or thoughts with the communityn          </p>n        </div>n      </div>nn      {/* Form */}n      <motion.divn        initial={{ opacity: 0, y: 20 }}n        animate={{ opacity: 1, y: 0 }}n        transition={{ delay: 0.1 }}n        className='card p-8'n      >n        <PostForm onSubmit={handleSubmit} isSubmitting={isSubmitting} />n      </motion.div>nn      {/* Tips Section */}n      <motion.divn        initial={{ opacity: 0, y: 20 }}n        animate={{ opacity: 1, y: 0 }}n        transition={{ delay: 0.2 }}n        className='bg-gradient-to-r from-primary-50 to-secondary-50 dark:from-primary-900/20 dark:to-secondary-900/20 rounded-xl p-6 border border-primary-200 dark:border-primary-800'n      >n        <h3 className='text-lg font-semibold text-gray-800 dark:text-gray-200 mb-4'>n          💡 Tips for Great Postsn        </h3>n        <div className='grid grid-cols-1 md:grid-cols-2 gap-4 text-sm text-gray-600 dark:text-gray-400'>n          <div className='space-y-2'>n            <p>✨ <strong>Be authentic</strong> - Share your real experiences and thoughts</p>n            <p>📸 <strong>Add images</strong> - Visuals make posts more engaging</p>n            <p>🏷️ <strong>Use tags</strong> - Help others discover your content</p>n          </div>n          <div className='space-y-2'>n            <p>💬 <strong>Be respectful</strong> - Create a positive community environment</p>n            <p>🎯 <strong>Be specific</strong> - Detailed stories are more interesting</p>n            <p>🌟 <strong>Have fun</strong> - Enjoy sharing with the community!</p>n          </div>n        </div>n      </motion.div>n    </motion.div>n  )n}nnexport default CreatePostPage